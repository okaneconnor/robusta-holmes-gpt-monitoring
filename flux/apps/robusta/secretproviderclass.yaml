apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: robusta-keyvault-sync
  namespace: robusta
spec:
  provider: azure

  # Sync secrets to Kubernetes Secret
  secretObjects:
    - secretName: robusta-slack-token
      type: Opaque
      data:
        - objectName: slack-bot-token  # Name of secret in Key Vault
          key: slack-api-key            # Key in Kubernetes Secret

  parameters:
    usePodIdentity: "false"
    useVMManagedIdentity: "false"
    clientID: "YOUR_WORKLOAD_IDENTITY_CLIENT_ID" # TODO: Replace this with your workload identity client ID (output from Terraform)

    # Key Vault details
    keyvaultName: "YOUR_KEYVAULT_NAME" # TODO: Replace this with your Key Vault name (output from Terraform, e.g., kv-robusta-dev)
    tenantId: "YOUR_AZURE_TENANT_ID" # TODO: Replace this with your Azure tenant ID

    objects: |
      array:
        - |
          objectName: slack-bot-token
          objectType: secret
          objectVersion: ""
